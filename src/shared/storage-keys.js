/**
 * Centralized storage key definitions
 * Used consistently across frontend and backend configuration
 */

export const STORAGE_KEYS = {
  TOKEN: 'regalem_token',
  USER: 'regalem_user',
};

export const { TOKEN: TOKEN_KEY, USER: USER_KEY } = STORAGE_KEYS;